const cosr =  require('express').Router(),
      request = require('superagent')
      mongodb = require('mongodb').MongoClient,
      url = 'mongodb://localhost/oms';

cost.get('/cost', (req,res)=>{

});

module.exports = cost;
